# Session-pooler connection string for the dev Supabase project (port 5432,
# session mode — supports `set role` and multi-statement transactions).
# Copy to .env (gitignored) and fill in the real password.
SUPABASE_DB_URL=postgresql://postgres.<ref>:<password>@aws-1-<region>.pooler.supabase.com:5432/postgres
