# Planova iOS v1 Core Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A working local-only SwiftUI iPhone/iPad app: multi-trip travel checklists (Prep / Itinerary / Packing) with editing, local-notification reminders, bilingual UI, and the 2026 China trip pre-seeded.

**Architecture:** Business logic lives in a local Swift package `PlanovaKit` (models, store, JSON persistence, seed loader, reminder planner) tested with `swift test` on macOS. The app target `Planova` is generated by XcodeGen and contains only SwiftUI views plus thin system integrations (`UNUserNotificationCenter`). Views read from an `@Observable` `AppModel` that wraps the store, debounced persistence, and reminder refresh.

**Tech Stack:** Swift 5.10, SwiftUI, Observation, XCTest, XcodeGen, UserNotifications. iOS/iPadOS 17 minimum. No third-party dependencies.

**Out of scope (follow-up plans):** CloudKit sync + iCloud family sharing (Plan 2), MapKit day maps + place attachment + TestFlight submission (Plan 3).

**Spec:** `docs/superpowers/specs/2026-06-10-planova-ios-design.md`

**Working directory:** repo root `/Users/wyu610/_Dev/Planova` unless a step says otherwise. All `swift test` commands run from `PlanovaKit/`.

---

## File structure

```
PlanovaKit/
  Package.swift
  Sources/PlanovaKit/
    Models.swift           // Trip, TripDay, ChecklistItem, Place, ItemKind, ItinerarySort
    TripStore.swift        // @Observable source of truth, intent methods
    TripRepository.swift   // JSON file persistence
    ReminderPlanner.swift  // pure reminder diffing logic
    SeedLoader.swift       // builds the China-2026 Trip from bundled JSON
    Resources/seed-china-2026.json
  Tests/PlanovaKitTests/
    ModelsTests.swift
    TripStoreTests.swift
    TripRepositoryTests.swift
    ReminderPlannerTests.swift
    SeedLoaderTests.swift
project.yml                // XcodeGen definition of the Planova app target
Planova/
  App/PlanovaApp.swift
  App/AppModel.swift
  Features/TripList/TripListView.swift     // + TripRowView, NewTripView
  Features/TripDetail/TripDetailView.swift
  Features/Checklist/ChecklistRow.swift    // + EditorContext
  Features/Checklist/PrepView.swift
  Features/Itinerary/ItineraryView.swift   // + DayHeader
  Features/Packing/PackingView.swift
  Features/ItemEditor/ItemEditorView.swift
  Reminders/ReminderScheduler.swift
  Resources/Localizable.xcstrings
```

---

### Task 1: PlanovaKit package scaffold

**Files:**
- Create: `PlanovaKit/Package.swift`
- Create: `PlanovaKit/Sources/PlanovaKit/Models.swift` (placeholder)
- Create: `PlanovaKit/Tests/PlanovaKitTests/ModelsTests.swift` (placeholder)
- Modify: `.gitignore`

- [ ] **Step 1: Create the package manifest**

`PlanovaKit/Package.swift`:

```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "PlanovaKit",
    defaultLocalization: "en",
    platforms: [.iOS(.v17), .macOS(.v14)],
    products: [
        .library(name: "PlanovaKit", targets: ["PlanovaKit"])
    ],
    targets: [
        .target(name: "PlanovaKit", resources: [.process("Resources")]),
        .testTarget(name: "PlanovaKitTests", dependencies: ["PlanovaKit"])
    ]
)
```

- [ ] **Step 2: Create placeholder source and test**

`PlanovaKit/Sources/PlanovaKit/Models.swift`:

```swift
import Foundation
// Models are added in Task 2.
```

`PlanovaKit/Tests/PlanovaKitTests/ModelsTests.swift`:

```swift
import XCTest
@testable import PlanovaKit

final class ModelsTests: XCTestCase {
    func testScaffold() {
        XCTAssertTrue(true)
    }
}
```

Also create `PlanovaKit/Sources/PlanovaKit/Resources/.gitkeep` (empty file) so the resources directory exists.

- [ ] **Step 3: Verify the package tests run**

Run: `cd PlanovaKit && swift test`
Expected: `Test Suite 'All tests' passed` (1 test).

- [ ] **Step 4: Update .gitignore**

Append to `.gitignore`:

```
.build/
DerivedData/
*.xcodeproj
*.xcuserstate
```

- [ ] **Step 5: Commit**

```bash
git add .gitignore PlanovaKit
git commit -m "feat: scaffold PlanovaKit Swift package"
```

---

### Task 2: Domain models + itinerary sort

**Files:**
- Modify: `PlanovaKit/Sources/PlanovaKit/Models.swift`
- Modify: `PlanovaKit/Tests/PlanovaKitTests/ModelsTests.swift`

- [ ] **Step 1: Write failing tests**

Replace `PlanovaKit/Tests/PlanovaKitTests/ModelsTests.swift` with:

```swift
import XCTest
@testable import PlanovaKit

final class ModelsTests: XCTestCase {

    func testTripCodableRoundTrip() throws {
        let day = TripDay(id: UUID(), date: Date(timeIntervalSince1970: 1_780_000_000), city: "上海", title: "抵达上海")
        let item = ChecklistItem(kind: .itinerary, label: "酒店早餐", dayID: day.id, time: "08:00", owner: "全家", sortOrder: 3)
        let trip = Trip(id: UUID(), name: "Test", startDate: Date(timeIntervalSince1970: 0),
                        endDate: Date(timeIntervalSince1970: 86_400), destinations: ["上海"],
                        days: [day], items: [item], schemaVersion: 1)
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        let decoded = try decoder.decode(Trip.self, from: encoder.encode(trip))
        XCTAssertEqual(decoded, trip)
    }

    func testChecklistItemDefaults() {
        let item = ChecklistItem(kind: .packing, label: "护照")
        XCTAssertFalse(item.isDone)
        XCTAssertNil(item.dayID)
        XCTAssertNil(item.reminderDate)
        XCTAssertEqual(item.notes, "")
    }

    func testDaySortedPutsTimedItemsFirstInTimeOrder() {
        let a = ChecklistItem(kind: .itinerary, label: "a", sortOrder: 0)
        let b = ChecklistItem(kind: .itinerary, label: "b", time: "14:00", sortOrder: 1)
        let c = ChecklistItem(kind: .itinerary, label: "c", time: "09:30", sortOrder: 2)
        let d = ChecklistItem(kind: .itinerary, label: "d", sortOrder: 3)
        let sorted = ItinerarySort.daySorted([a, b, c, d])
        XCTAssertEqual(sorted.map(\.label), ["c", "b", "a", "d"])
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd PlanovaKit && swift test`
Expected: compile FAILURE — `cannot find 'TripDay' in scope` etc.

- [ ] **Step 3: Implement the models**

Replace `PlanovaKit/Sources/PlanovaKit/Models.swift` with:

```swift
import Foundation

public enum ItemKind: String, Codable, CaseIterable, Sendable {
    case prep, hotel, doc, itinerary, packing
}

public struct Place: Codable, Equatable, Sendable {
    public var name: String
    public var query: String
    public var latitude: Double?
    public var longitude: Double?

    public init(name: String, query: String, latitude: Double? = nil, longitude: Double? = nil) {
        self.name = name
        self.query = query
        self.latitude = latitude
        self.longitude = longitude
    }
}

public struct ChecklistItem: Codable, Equatable, Identifiable, Sendable {
    public var id: UUID
    public var kind: ItemKind
    public var label: String
    public var notes: String
    public var dayID: UUID?
    public var time: String?      // "HH:mm", itinerary items only
    public var owner: String?
    public var isDone: Bool
    public var sortOrder: Int
    public var reminderDate: Date?
    public var place: Place?
    public var modifiedAt: Date

    public init(id: UUID = UUID(), kind: ItemKind, label: String, notes: String = "",
                dayID: UUID? = nil, time: String? = nil, owner: String? = nil,
                isDone: Bool = false, sortOrder: Int = 0, reminderDate: Date? = nil,
                place: Place? = nil, modifiedAt: Date = Date(timeIntervalSince1970: 0)) {
        self.id = id
        self.kind = kind
        self.label = label
        self.notes = notes
        self.dayID = dayID
        self.time = time
        self.owner = owner
        self.isDone = isDone
        self.sortOrder = sortOrder
        self.reminderDate = reminderDate
        self.place = place
        self.modifiedAt = modifiedAt
    }
}

public struct TripDay: Codable, Equatable, Identifiable, Sendable {
    public var id: UUID
    public var date: Date
    public var city: String
    public var title: String

    public init(id: UUID = UUID(), date: Date, city: String, title: String) {
        self.id = id
        self.date = date
        self.city = city
        self.title = title
    }
}

public struct Trip: Codable, Equatable, Identifiable, Sendable {
    public var id: UUID
    public var name: String
    public var startDate: Date
    public var endDate: Date
    public var destinations: [String]
    public var days: [TripDay]
    public var items: [ChecklistItem]
    public var schemaVersion: Int

    public init(id: UUID = UUID(), name: String, startDate: Date, endDate: Date,
                destinations: [String] = [], days: [TripDay] = [],
                items: [ChecklistItem] = [], schemaVersion: Int = 1) {
        self.id = id
        self.name = name
        self.startDate = startDate
        self.endDate = endDate
        self.destinations = destinations
        self.days = days
        self.items = items
        self.schemaVersion = schemaVersion
    }
}

/// Port of the web app's sortDayItems(): timed items first in time order,
/// then untimed items in their original order.
public enum ItinerarySort {
    public static func daySorted(_ items: [ChecklistItem]) -> [ChecklistItem] {
        items.enumerated().sorted { a, b in
            let ta = a.element.time ?? ""
            let tb = b.element.time ?? ""
            switch (!ta.isEmpty, !tb.isEmpty) {
            case (true, true):
                if ta != tb { return ta < tb }
                return a.offset < b.offset
            case (true, false): return true
            case (false, true): return false
            case (false, false): return a.offset < b.offset
            }
        }.map(\.element)
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd PlanovaKit && swift test`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add PlanovaKit
git commit -m "feat: add domain models and itinerary sorting"
```

---

### Task 3: Seed data + SeedLoader

**Files:**
- Create: `PlanovaKit/Sources/PlanovaKit/Resources/seed-china-2026.json`
- Create: `PlanovaKit/Sources/PlanovaKit/SeedLoader.swift`
- Create: `PlanovaKit/Tests/PlanovaKitTests/SeedLoaderTests.swift`
- Delete: `PlanovaKit/Sources/PlanovaKit/Resources/.gitkeep`

- [ ] **Step 1: Create the seed JSON**

`PlanovaKit/Sources/PlanovaKit/Resources/seed-china-2026.json` — content ported from `trip-webapp/index.html` constants `PREP` (lines 528–544), `HOTELS` (546–553), `DOCS` (555–561), `DAYS` (563–585), `PACK` (587):

```json
{
  "name": "2026 暑假中国行",
  "start": "2026-07-11",
  "end": "2026-07-31",
  "destinations": ["上海", "香港", "广州"],
  "prep": [
    { "label": "上海天文馆门票（7/12 参观）", "notes": "门票仅在参观日前 3 天（含当日）开售，每日限约 7000 人、易售罄。请 7月10日 09:30 准时在官方“上海科技馆自然博物馆天文馆门票”小程序抢票，勿用第三方平台。" },
    { "label": "高铁票：上海虹桥 → 香港西九龙（7/16）", "notes": "跨境高铁预售期 15 天（含当天），约 7月1日 起可在 12306 购买。" },
    { "label": "高铁票：香港西九龙 → 广州（7/19）", "notes": "同为跨境票，预售期 15 天，约 7月4日 起可购。" },
    { "label": "香港海洋公园门票（7/18）", "notes": "建议官网/APP 提前购票；并下载 Ocean Park App 看排队与表演时间。" },
    { "label": "长隆野生动物世界门票（7/22）", "notes": "旺季人多，官方渠道提前购票更稳妥。" }
  ],
  "hotels": [
    { "label": "上海 · 7/11 – 7/13", "notes": "金桥海昌企鹅度假酒店", "done": true },
    { "label": "上海 · 7/13 – 7/16", "notes": "世博陆家嘴滨江中心亚朵 S 酒店 · 已选定 · 确认预订" },
    { "label": "香港 · 7/16 – 7/19", "notes": "待预订" },
    { "label": "广州 · 7/19 – 7/27", "notes": "待预订（8 晚）" },
    { "label": "揭西 · 7/27 – 7/28", "notes": "待预订（探亲 1 晚）" },
    { "label": "广州 · 7/28 – 7/31", "notes": "待预订" }
  ],
  "docs": [
    { "label": "4 人护照检查（有效期建议 6 个月以上）" },
    { "label": "入境证件确认：港澳通行证/签注 或 护照+签证（按身份核对）", "notes": "内地↔香港往返需对应有效证件，出行前务必确认。" },
    { "label": "往返航班确认：7/11 抵上海浦东 · 7/31 广州白云离境" },
    { "label": "手机支付与网络：支付宝/微信绑卡、内地漫游或 eSIM / 大湾区数据" },
    { "label": "健康申报：常规情况已无需填报，仅有发热等症状时主动向海关申报", "notes": "自 2023年11月起取消强制健康申报卡。" }
  ],
  "pack": ["护照／证件", "手机", "充电宝", "雨伞", "防晒霜", "纸巾", "水", "少量现金", "酒店房卡", "高铁／门票二维码"],
  "days": [
    { "date": "2026-07-11", "city": "上海", "title": "抵达上海", "items": ["抵达上海浦东国际机场", "办理入境与行李提取", "前往金桥海昌企鹅度假酒店", "办理入住手续"] },
    { "date": "2026-07-12", "city": "上海", "title": "滴水湖科技探索日", "items": ["酒店早餐", "前往上海天文馆", "参观上海天文馆（需提前抢票）", "朵云书院·滴水湖店", "滴水湖环湖骑行／驾车", "七彩桥", "滴水湖广场亲水平台", "未来明信片体验", "耀雪冰雪世界", "返回酒店休息"] },
    { "date": "2026-07-13", "city": "上海", "title": "换酒店 · 陆家嘴轻松日", "items": ["酒店早餐", "退房：企鹅度假酒店", "磁浮列车（地铁 16→2 号线）", "前往亚朵 S 酒店 · 入住/寄存行李", "陆家嘴午餐", "陆家嘴滨江步道", "东方明珠", "前往外滩 · 黄浦江景", "北外滩滨江步道 · 浦东日落", "正大广场晚餐", "返回酒店休息"] },
    { "date": "2026-07-14", "city": "上海", "title": "朱家角古镇慢游日", "items": ["酒店早餐", "前往朱家角古镇 · 漫步", "放生桥", "北大街老街", "古镇特色午餐", "品尝扎肉", "品尝阿婆粽", "江南小吃", "朱家角古镇游船", "朵云轩书局（可选）", "返回上海市区", "黄浦江夜游（夜景版·可选）"] },
    { "date": "2026-07-15", "city": "上海", "title": "汽车文化与赛车体验日", "items": ["酒店早餐", "上海汽车博物馆", "汽车历史展区", "经典名车展区", "互动体验区", "安亭汽车城午餐", "锐擎卡丁车主题乐园", "卡丁车体验", "徐家汇商圈 · 尤尼克斯旗舰店", "返回酒店 · 整理行李", "检查高铁车票与护照证件"] },
    { "date": "2026-07-16", "city": "上海→香港", "title": "上海 → 香港", "items": ["酒店早餐 · 退房", "前往上海虹桥站", "预留充足安检时间", "搭高铁前往香港西九龙站", "抵达香港 · 入住酒店", "附近简单晚餐"] },
    { "date": "2026-07-17", "city": "香港", "title": "香港经典景点", "items": ["酒店早餐", "旺角 · 金鱼街", "波鞋街", "旺角午餐", "尖沙咀海滨长廊", "香港星光大道 · 维多利亚港", "天星小轮（尖沙咀→中环）", "太平山顶 · 维港夜景", "返回酒店"] },
    { "date": "2026-07-18", "city": "香港", "title": "香港海洋公园", "items": ["酒店早餐", "前往香港海洋公园", "动物展区", "机动游戏", "缆车体验", "园区午餐", "继续玩", "返回酒店 · 早点休息"] },
    { "date": "2026-07-19", "city": "香港→广州", "title": "探险公园 → 广州", "items": ["酒店早餐", "Go Green Adventure Park HK 户外探险", "返回酒店领取行李", "前往香港西九龙站", "搭高铁前往广州", "抵达广州 · 入住酒店", "简单晚餐 · 早点休息"] },
    { "date": "2026-07-20", "city": "广州", "title": "广州城市地标", "items": ["酒店早餐", "广州塔 · 外观拍照", "花城广场", "海心沙（可选）", "附近晚餐", "珠江夜游", "返回酒店"] },
    { "date": "2026-07-21", "city": "广州", "title": "老广州文化探索", "items": ["酒店早餐", "北京路步行街", "广式早茶", "上下九步行街", "永庆坊", "沙面岛", "咖啡馆休息", "返回酒店"] },
    { "date": "2026-07-22", "city": "广州", "title": "长隆野生动物世界", "items": ["酒店早餐", "前往长隆野生动物世界", "小火车／自驾区", "步行游览区", "动物表演", "园区午餐", "继续玩", "返回酒店 · 早点休息"] },
    { "date": "2026-07-23", "city": "广州", "title": "购物与卡丁车", "items": ["酒店早餐", "APM 商场 · 购物", "午餐", "一起卡丁赛车乐园 · 卡丁车体验", "自由活动", "返回酒店"] },
    { "date": "2026-07-24", "city": "广州·顺德", "title": "顺德美食一日游", "items": ["酒店早餐", "前往顺德", "华盖路步行街", "品尝双皮奶", "顺德鱼生", "烧鹅", "顺峰山公园", "返回广州酒店休息"] },
    { "date": "2026-07-25", "city": "广州·开平", "title": "开平侨乡文化一日游", "items": ["酒店早餐", "前往开平", "开平碉楼 · 碉楼群", "赤坎古镇", "午餐", "华侨文化体验", "返回广州酒店休息"] },
    { "date": "2026-07-26", "city": "广州", "title": "休息调整日（缓冲）", "items": ["睡到自然醒 · 酒店早餐", "酒店休息", "洗衣／整理衣物", "补充购物", "太古汇（可选）", "正佳广场（可选）", "天河城（可选）", "简单晚餐 · 为揭西做准备"] },
    { "date": "2026-07-27", "city": "揭西", "title": "广州 → 揭西（探亲）", "items": ["酒店早餐", "整理过夜行李", "前往揭西县", "潮汕特色美食", "欣赏家乡风光", "入住揭西住宿"] },
    { "date": "2026-07-28", "city": "揭西→广州", "title": "揭西 → 广州 · 返回调整", "items": ["揭西早餐", "继续探访", "整理行李", "返回广州 · 入住酒店", "下午休息 · 自由活动", "补拍照片 · 补充购物"] },
    { "date": "2026-07-29", "city": "广州", "title": "广州美食探索日", "items": ["酒店早餐或早茶", "广式早茶", "老字号餐厅", "广州特色小吃", "自由逛街", "家庭聚餐餐厅", "返回酒店"] },
    { "date": "2026-07-30", "city": "广州", "title": "最后采购 · 整理行李", "items": ["酒店早餐", "购买广州伴手礼", "整理行李", "检查护照与证件", "确认航班", "检查托运行李重量", "安排机场交通", "早点休息"] },
    { "date": "2026-07-31", "city": "广州→加拿大", "title": "广州返程", "items": ["酒店早餐 · 退房", "前往广州白云国际机场", "办理登机手续", "托运行李 · 安检", "返回加拿大 ✈"] }
  ]
}
```

Delete `PlanovaKit/Sources/PlanovaKit/Resources/.gitkeep`.

- [ ] **Step 2: Write failing tests**

`PlanovaKit/Tests/PlanovaKitTests/SeedLoaderTests.swift`:

```swift
import XCTest
@testable import PlanovaKit

final class SeedLoaderTests: XCTestCase {

    func testSeedLoadsChinaTrip() throws {
        let trip = try SeedLoader.loadChinaTrip2026()
        XCTAssertEqual(trip.name, "2026 暑假中国行")
        XCTAssertEqual(trip.destinations, ["上海", "香港", "广州"])
        XCTAssertEqual(trip.days.count, 21)
        XCTAssertEqual(trip.items.count, 188)
        XCTAssertEqual(trip.items.filter { $0.kind == .prep }.count, 5)
        XCTAssertEqual(trip.items.filter { $0.kind == .hotel }.count, 6)
        XCTAssertEqual(trip.items.filter { $0.kind == .doc }.count, 5)
        XCTAssertEqual(trip.items.filter { $0.kind == .packing }.count, 10)
        XCTAssertEqual(trip.items.filter { $0.kind == .itinerary }.count, 162)
    }

    func testFirstHotelIsBookedAndDayItemsLinkToDays() throws {
        let trip = try SeedLoader.loadChinaTrip2026()
        let hotels = trip.items.filter { $0.kind == .hotel }.sorted { $0.sortOrder < $1.sortOrder }
        XCTAssertTrue(hotels[0].isDone)
        XCTAssertFalse(hotels[1].isDone)

        let day12 = trip.days[1]
        XCTAssertEqual(day12.title, "滴水湖科技探索日")
        let day12Items = trip.items.filter { $0.dayID == day12.id }
        XCTAssertEqual(day12Items.count, 10)

        let dayIDs = Set(trip.days.map(\.id))
        for item in trip.items where item.kind == .itinerary {
            XCTAssertNotNil(item.dayID)
            XCTAssertTrue(dayIDs.contains(item.dayID!))
        }
    }
}
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `cd PlanovaKit && swift test`
Expected: compile FAILURE — `cannot find 'SeedLoader' in scope`.

- [ ] **Step 4: Implement SeedLoader**

`PlanovaKit/Sources/PlanovaKit/SeedLoader.swift`:

```swift
import Foundation

struct SeedFile: Decodable {
    struct SeedItem: Decodable {
        var label: String
        var notes: String?
        var done: Bool?
    }
    struct SeedDay: Decodable {
        var date: String
        var city: String
        var title: String
        var items: [String]
    }
    var name: String
    var start: String
    var end: String
    var destinations: [String]
    var prep: [SeedItem]
    var hotels: [SeedItem]
    var docs: [SeedItem]
    var pack: [String]
    var days: [SeedDay]
}

public enum SeedLoader {

    public static func loadChinaTrip2026() throws -> Trip {
        guard let url = Bundle.module.url(forResource: "seed-china-2026", withExtension: "json") else {
            throw CocoaError(.fileNoSuchFile)
        }
        let file = try JSONDecoder().decode(SeedFile.self, from: Data(contentsOf: url))
        return try makeTrip(from: file)
    }

    static func makeTrip(from file: SeedFile) throws -> Trip {
        guard let start = parseDay(file.start), let end = parseDay(file.end) else {
            throw CocoaError(.coderInvalidValue)
        }
        var items: [ChecklistItem] = []
        func add(_ seeds: [SeedFile.SeedItem], kind: ItemKind) {
            for (i, s) in seeds.enumerated() {
                items.append(ChecklistItem(kind: kind, label: s.label, notes: s.notes ?? "",
                                           isDone: s.done ?? false, sortOrder: i))
            }
        }
        add(file.prep, kind: .prep)
        add(file.hotels, kind: .hotel)
        add(file.docs, kind: .doc)
        for (i, label) in file.pack.enumerated() {
            items.append(ChecklistItem(kind: .packing, label: label, sortOrder: i))
        }
        var days: [TripDay] = []
        var order = 0
        for d in file.days {
            guard let date = parseDay(d.date) else { throw CocoaError(.coderInvalidValue) }
            let day = TripDay(date: date, city: d.city, title: d.title)
            days.append(day)
            for label in d.items {
                items.append(ChecklistItem(kind: .itinerary, label: label, dayID: day.id, sortOrder: order))
                order += 1
            }
        }
        return Trip(name: file.name, startDate: start, endDate: end,
                    destinations: file.destinations, days: days, items: items, schemaVersion: 1)
    }

    static func parseDay(_ s: String) -> Date? {
        let f = DateFormatter()
        f.calendar = Calendar(identifier: .gregorian)
        f.dateFormat = "yyyy-MM-dd"
        f.timeZone = .current
        return f.date(from: s)
    }
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `cd PlanovaKit && swift test`
Expected: PASS (5 tests).

- [ ] **Step 6: Commit**

```bash
git add PlanovaKit
git commit -m "feat: add China 2026 seed data and loader"
```

---

### Task 4: TripStore

**Files:**
- Create: `PlanovaKit/Sources/PlanovaKit/TripStore.swift`
- Create: `PlanovaKit/Tests/PlanovaKitTests/TripStoreTests.swift`

- [ ] **Step 1: Write failing tests**

`PlanovaKit/Tests/PlanovaKitTests/TripStoreTests.swift`:

```swift
import XCTest
@testable import PlanovaKit

final class TripStoreTests: XCTestCase {

    private func makeTrip() -> Trip {
        Trip(name: "T", startDate: Date(timeIntervalSince1970: 0), endDate: Date(timeIntervalSince1970: 86_400),
             items: [
                ChecklistItem(kind: .prep, label: "p0", sortOrder: 0),
                ChecklistItem(kind: .packing, label: "k0", isDone: true, sortOrder: 0),
                ChecklistItem(kind: .packing, label: "k1", isDone: true, sortOrder: 1)
             ])
    }

    func testToggleFlipsDoneAndStampsModifiedAtAndNotifies() {
        let trip = makeTrip()
        let store = TripStore(trips: [trip])
        var changed: Trip?
        store.onChange = { changed = $0 }
        let now = Date(timeIntervalSince1970: 1000)

        store.toggle(itemID: trip.items[0].id, in: trip.id, now: now)

        let item = store.trip(id: trip.id)!.items[0]
        XCTAssertTrue(item.isDone)
        XCTAssertEqual(item.modifiedAt, now)
        XCTAssertEqual(changed?.id, trip.id)
    }

    func testAddItemAppendsWithNextSortOrderForItsKind() {
        let trip = makeTrip()
        let store = TripStore(trips: [trip])

        store.addItem(ChecklistItem(kind: .packing, label: "k2"), to: trip.id)

        let packing = store.trip(id: trip.id)!.items.filter { $0.kind == .packing }
        XCTAssertEqual(packing.count, 3)
        XCTAssertEqual(packing.map(\.sortOrder).max(), 2)
    }

    func testUpdateItemReplacesFields() {
        let trip = makeTrip()
        let store = TripStore(trips: [trip])
        var edited = trip.items[0]
        edited.label = "renamed"
        edited.owner = "妈妈"

        store.updateItem(edited, in: trip.id, now: Date(timeIntervalSince1970: 2000))

        let item = store.trip(id: trip.id)!.items[0]
        XCTAssertEqual(item.label, "renamed")
        XCTAssertEqual(item.owner, "妈妈")
        XCTAssertEqual(item.modifiedAt, Date(timeIntervalSince1970: 2000))
    }

    func testDeleteItemRemovesIt() {
        let trip = makeTrip()
        let store = TripStore(trips: [trip])

        store.deleteItem(id: trip.items[0].id, in: trip.id)

        XCTAssertEqual(store.trip(id: trip.id)!.items.count, 2)
    }

    func testResetPackingClearsOnlyPackingDoneFlags() {
        var trip = makeTrip()
        trip.items[0].isDone = true   // prep item stays done
        let store = TripStore(trips: [trip])

        store.resetPacking(in: trip.id)

        let items = store.trip(id: trip.id)!.items
        XCTAssertTrue(items.first { $0.kind == .prep }!.isDone)
        XCTAssertTrue(items.filter { $0.kind == .packing }.allSatisfy { !$0.isDone })
    }

    func testTripsSortedByStartDate() {
        let early = Trip(name: "early", startDate: Date(timeIntervalSince1970: 0), endDate: Date(timeIntervalSince1970: 1))
        let late = Trip(name: "late", startDate: Date(timeIntervalSince1970: 9999), endDate: Date(timeIntervalSince1970: 10_000))
        let store = TripStore(trips: [late])
        store.addTrip(early)
        XCTAssertEqual(store.trips.map(\.name), ["early", "late"])
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd PlanovaKit && swift test`
Expected: compile FAILURE — `cannot find 'TripStore' in scope`.

- [ ] **Step 3: Implement TripStore**

`PlanovaKit/Sources/PlanovaKit/TripStore.swift`:

```swift
import Foundation
import Observation

@Observable
public final class TripStore {
    public private(set) var trips: [Trip]

    /// Called with the changed trip after every mutation; the app layer
    /// hooks persistence (and later, sync) here.
    @ObservationIgnored public var onChange: ((Trip) -> Void)?

    public init(trips: [Trip] = []) {
        self.trips = trips.sorted { $0.startDate < $1.startDate }
    }

    public func trip(id: UUID) -> Trip? {
        trips.first { $0.id == id }
    }

    public func addTrip(_ trip: Trip) {
        trips.append(trip)
        trips.sort { $0.startDate < $1.startDate }
        onChange?(trip)
    }

    /// Removal from disk is the caller's responsibility (no trip left to save).
    public func deleteTrip(id: UUID) {
        trips.removeAll { $0.id == id }
    }

    private func mutate(_ tripID: UUID, _ change: (inout Trip) -> Void) {
        guard let i = trips.firstIndex(where: { $0.id == tripID }) else { return }
        change(&trips[i])
        onChange?(trips[i])
    }

    public func toggle(itemID: UUID, in tripID: UUID, now: Date = Date()) {
        mutate(tripID) { trip in
            guard let j = trip.items.firstIndex(where: { $0.id == itemID }) else { return }
            trip.items[j].isDone.toggle()
            trip.items[j].modifiedAt = now
        }
    }

    public func addItem(_ item: ChecklistItem, to tripID: UUID, now: Date = Date()) {
        mutate(tripID) { trip in
            var item = item
            item.sortOrder = (trip.items.filter { $0.kind == item.kind }.map(\.sortOrder).max() ?? -1) + 1
            item.modifiedAt = now
            trip.items.append(item)
        }
    }

    public func updateItem(_ item: ChecklistItem, in tripID: UUID, now: Date = Date()) {
        mutate(tripID) { trip in
            guard let j = trip.items.firstIndex(where: { $0.id == item.id }) else { return }
            var item = item
            item.modifiedAt = now
            trip.items[j] = item
        }
    }

    public func deleteItem(id: UUID, in tripID: UUID) {
        mutate(tripID) { $0.items.removeAll { $0.id == id } }
    }

    public func resetPacking(in tripID: UUID, now: Date = Date()) {
        mutate(tripID) { trip in
            for j in trip.items.indices where trip.items[j].kind == .packing && trip.items[j].isDone {
                trip.items[j].isDone = false
                trip.items[j].modifiedAt = now
            }
        }
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd PlanovaKit && swift test`
Expected: PASS (11 tests).

- [ ] **Step 5: Commit**

```bash
git add PlanovaKit
git commit -m "feat: add TripStore with intent mutations"
```

---

### Task 5: TripRepository (JSON persistence)

**Files:**
- Create: `PlanovaKit/Sources/PlanovaKit/TripRepository.swift`
- Create: `PlanovaKit/Tests/PlanovaKitTests/TripRepositoryTests.swift`

- [ ] **Step 1: Write failing tests**

`PlanovaKit/Tests/PlanovaKitTests/TripRepositoryTests.swift`:

```swift
import XCTest
@testable import PlanovaKit

final class TripRepositoryTests: XCTestCase {

    private var tempDir: URL!

    override func setUpWithError() throws {
        tempDir = FileManager.default.temporaryDirectory
            .appendingPathComponent("planova-tests-\(UUID().uuidString)")
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: tempDir)
    }

    func testSaveAndLoadRoundTrip() throws {
        let repo = TripRepository(directory: tempDir)
        let trip = Trip(name: "T", startDate: Date(timeIntervalSince1970: 0),
                        endDate: Date(timeIntervalSince1970: 86_400),
                        items: [ChecklistItem(kind: .prep, label: "p")])

        try repo.save(trip)
        let loaded = try repo.loadAll()

        XCTAssertEqual(loaded, [trip])
    }

    func testLoadAllFromMissingDirectoryReturnsEmpty() throws {
        let repo = TripRepository(directory: tempDir)
        XCTAssertEqual(try repo.loadAll(), [])
    }

    func testDeleteRemovesFile() throws {
        let repo = TripRepository(directory: tempDir)
        let trip = Trip(name: "T", startDate: Date(timeIntervalSince1970: 0), endDate: Date(timeIntervalSince1970: 1))
        try repo.save(trip)

        try repo.delete(id: trip.id)

        XCTAssertEqual(try repo.loadAll(), [])
    }

    func testSaveOverwritesExisting() throws {
        let repo = TripRepository(directory: tempDir)
        var trip = Trip(name: "T", startDate: Date(timeIntervalSince1970: 0), endDate: Date(timeIntervalSince1970: 1))
        try repo.save(trip)
        trip.name = "Renamed"

        try repo.save(trip)

        XCTAssertEqual(try repo.loadAll().first?.name, "Renamed")
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd PlanovaKit && swift test`
Expected: compile FAILURE — `cannot find 'TripRepository' in scope`.

- [ ] **Step 3: Implement TripRepository**

`PlanovaKit/Sources/PlanovaKit/TripRepository.swift`:

```swift
import Foundation

/// One JSON document per trip: <directory>/<trip-uuid>.json
public struct TripRepository {
    public let directory: URL

    public init(directory: URL) {
        self.directory = directory
    }

    private var encoder: JSONEncoder {
        let e = JSONEncoder()
        e.dateEncodingStrategy = .iso8601
        e.outputFormatting = [.prettyPrinted, .sortedKeys]
        return e
    }

    private var decoder: JSONDecoder {
        let d = JSONDecoder()
        d.dateDecodingStrategy = .iso8601
        return d
    }

    public func loadAll() throws -> [Trip] {
        guard FileManager.default.fileExists(atPath: directory.path) else { return [] }
        return try FileManager.default
            .contentsOfDirectory(at: directory, includingPropertiesForKeys: nil)
            .filter { $0.pathExtension == "json" }
            .map { try decoder.decode(Trip.self, from: try Data(contentsOf: $0)) }
    }

    public func save(_ trip: Trip) throws {
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
        let url = directory.appendingPathComponent("\(trip.id.uuidString).json")
        try encoder.encode(trip).write(to: url, options: .atomic)
    }

    public func delete(id: UUID) throws {
        let url = directory.appendingPathComponent("\(id.uuidString).json")
        if FileManager.default.fileExists(atPath: url.path) {
            try FileManager.default.removeItem(at: url)
        }
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd PlanovaKit && swift test`
Expected: PASS (15 tests).

- [ ] **Step 5: Commit**

```bash
git add PlanovaKit
git commit -m "feat: add JSON file trip repository"
```

---

### Task 6: ReminderPlanner

**Files:**
- Create: `PlanovaKit/Sources/PlanovaKit/ReminderPlanner.swift`
- Create: `PlanovaKit/Tests/PlanovaKitTests/ReminderPlannerTests.swift`

- [ ] **Step 1: Write failing tests**

`PlanovaKit/Tests/PlanovaKitTests/ReminderPlannerTests.swift`:

```swift
import XCTest
@testable import PlanovaKit

final class ReminderPlannerTests: XCTestCase {

    private let now = Date(timeIntervalSince1970: 1_000_000)

    private func trip(items: [ChecklistItem]) -> Trip {
        Trip(name: "China", startDate: now, endDate: now.addingTimeInterval(86_400), items: items)
    }

    func testDesiredIncludesOnlyFutureUndoneReminders() {
        let future = ChecklistItem(kind: .prep, label: "抢票", reminderDate: now.addingTimeInterval(3600))
        let past = ChecklistItem(kind: .prep, label: "old", reminderDate: now.addingTimeInterval(-3600))
        let done = ChecklistItem(kind: .prep, label: "done", isDone: true, reminderDate: now.addingTimeInterval(3600))
        let none = ChecklistItem(kind: .prep, label: "none")

        let desired = ReminderPlanner.desiredReminders(for: [trip(items: [future, past, done, none])], now: now)

        XCTAssertEqual(desired.count, 1)
        XCTAssertEqual(desired[0].id, future.id.uuidString)
        XCTAssertEqual(desired[0].title, "抢票")
        XCTAssertEqual(desired[0].body, "China")
        XCTAssertEqual(desired[0].date, now.addingTimeInterval(3600))
    }

    func testDiffCancelsStaleAndSchedulesAllDesired() {
        let item = ChecklistItem(kind: .prep, label: "a", reminderDate: now.addingTimeInterval(60))
        let desired = ReminderPlanner.desiredReminders(for: [trip(items: [item])], now: now)
        let pending: Set<String> = [item.id.uuidString, "stale-id"]

        let plan = ReminderPlanner.diff(desired: desired, pendingIDs: pending)

        XCTAssertEqual(plan.cancel, ["stale-id"])
        XCTAssertEqual(plan.schedule, desired)
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd PlanovaKit && swift test`
Expected: compile FAILURE — `cannot find 'ReminderPlanner' in scope`.

- [ ] **Step 3: Implement ReminderPlanner**

`PlanovaKit/Sources/PlanovaKit/ReminderPlanner.swift`:

```swift
import Foundation

public struct PlannedReminder: Equatable, Sendable {
    public let id: String      // ChecklistItem UUID string == notification identifier
    public let date: Date
    public let title: String
    public let body: String

    public init(id: String, date: Date, title: String, body: String) {
        self.id = id
        self.date = date
        self.title = title
        self.body = body
    }
}

public enum ReminderPlanner {

    public static func desiredReminders(for trips: [Trip], now: Date) -> [PlannedReminder] {
        trips.flatMap { trip in
            trip.items.compactMap { item -> PlannedReminder? in
                guard let date = item.reminderDate, date > now, !item.isDone else { return nil }
                return PlannedReminder(id: item.id.uuidString, date: date, title: item.label, body: trip.name)
            }
        }
    }

    /// Scheduling a request with an identifier that is already pending
    /// replaces it, so `schedule` is simply every desired reminder; only
    /// stale identifiers need explicit cancellation.
    public static func diff(desired: [PlannedReminder], pendingIDs: Set<String>)
        -> (cancel: [String], schedule: [PlannedReminder]) {
        let desiredIDs = Set(desired.map(\.id))
        return (pendingIDs.subtracting(desiredIDs).sorted(), desired)
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd PlanovaKit && swift test`
Expected: PASS (17 tests).

- [ ] **Step 5: Commit**

```bash
git add PlanovaKit
git commit -m "feat: add reminder planning logic"
```

---

### Task 7: App scaffold (XcodeGen + AppModel + entry point)

**Files:**
- Create: `project.yml`
- Create: `Planova/App/PlanovaApp.swift`
- Create: `Planova/App/AppModel.swift`
- Create: `Planova/Features/TripList/TripListView.swift` (placeholder)

- [ ] **Step 1: Install XcodeGen if missing**

Run: `which xcodegen || brew install xcodegen`
Expected: a path like `/opt/homebrew/bin/xcodegen`.

- [ ] **Step 2: Create project.yml**

`project.yml`:

```yaml
name: Planova
options:
  bundleIdPrefix: com.planova
  deploymentTarget:
    iOS: "17.0"
packages:
  PlanovaKit:
    path: PlanovaKit
targets:
  Planova:
    type: application
    platform: iOS
    sources: [Planova]
    dependencies:
      - package: PlanovaKit
    scheme: {}
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1,2"
        SWIFT_VERSION: "5.10"
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_CFBundleDisplayName: Planova
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 0.1.0
```

- [ ] **Step 3: Create the app entry, model, and placeholder root view**

`Planova/App/PlanovaApp.swift`:

```swift
import SwiftUI

@main
struct PlanovaApp: App {
    @State private var model = AppModel()

    var body: some Scene {
        WindowGroup {
            TripListView()
                .environment(model)
        }
    }
}
```

`Planova/App/AppModel.swift`:

```swift
import Foundation
import Observation
import PlanovaKit

@MainActor
@Observable
final class AppModel {
    let store: TripStore
    private let repository: TripRepository
    @ObservationIgnored private var saveTasks: [UUID: Task<Void, Never>] = [:]

    init() {
        let dir = URL.applicationSupportDirectory.appendingPathComponent("trips")
        self.repository = TripRepository(directory: dir)
        let trips = (try? repository.loadAll()) ?? []
        self.store = TripStore(trips: trips)
        seedIfNeeded()
        store.onChange = { [weak self] trip in self?.scheduleSave(trip) }
    }

    // MARK: - Intents (views call these, not the store directly)

    func addTrip(_ trip: Trip) {
        store.addTrip(trip)
        try? repository.save(trip)
    }

    func deleteTrip(id: UUID) {
        store.deleteTrip(id: id)
        try? repository.delete(id: id)
        refreshReminders()
    }

    func toggle(itemID: UUID, in tripID: UUID) {
        store.toggle(itemID: itemID, in: tripID)
        refreshReminders()
    }

    func addItem(_ item: ChecklistItem, to tripID: UUID) {
        store.addItem(item, to: tripID)
        refreshReminders()
    }

    func updateItem(_ item: ChecklistItem, in tripID: UUID) {
        store.updateItem(item, in: tripID)
        refreshReminders()
    }

    func deleteItem(id: UUID, in tripID: UUID) {
        store.deleteItem(id: id, in: tripID)
        refreshReminders()
    }

    func resetPacking(in tripID: UUID) {
        store.resetPacking(in: tripID)
    }

    // MARK: - Persistence (debounced, mirrors the PWA's 150 ms save)

    private func scheduleSave(_ trip: Trip) {
        saveTasks[trip.id]?.cancel()
        saveTasks[trip.id] = Task { [repository] in
            try? await Task.sleep(for: .milliseconds(150))
            guard !Task.isCancelled else { return }
            try? repository.save(trip)
        }
    }

    // MARK: - Seed

    private func seedIfNeeded() {
        let key = "didSeedChinaTrip2026"
        guard !UserDefaults.standard.bool(forKey: key) else { return }
        if let trip = try? SeedLoader.loadChinaTrip2026() {
            store.addTrip(trip)
            try? repository.save(trip)
        }
        UserDefaults.standard.set(true, forKey: key)
    }

    // MARK: - Reminders (ReminderScheduler is added in Task 9)

    func refreshReminders() {
        let trips = store.trips
        Task { await ReminderScheduler.refresh(trips: trips) }
    }
}
```

`Planova/Reminders/ReminderScheduler.swift` (minimal stub now, real implementation in Task 9 — this keeps Task 7 compiling):

```swift
import Foundation
import PlanovaKit

enum ReminderScheduler {
    static func refresh(trips: [Trip]) async {
        // Implemented in Task 9.
    }
}
```

`Planova/Features/TripList/TripListView.swift` (placeholder, replaced in Task 8):

```swift
import SwiftUI
import PlanovaKit

struct TripListView: View {
    @Environment(AppModel.self) private var model

    var body: some View {
        Text("Planova — \(model.store.trips.count) trips")
    }
}
```

- [ ] **Step 4: Generate the project and build**

Run:
```bash
xcodegen generate
xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`. (If the scheme is missing, run `xcodegen generate` again and check `scheme: {}` is present under the target.)

- [ ] **Step 5: Commit**

```bash
git add project.yml Planova
git commit -m "feat: scaffold Planova app target with XcodeGen and AppModel"
```

---

### Task 8: Trip list + new trip

**Files:**
- Modify: `Planova/Features/TripList/TripListView.swift` (replace placeholder)

- [ ] **Step 1: Implement the trip list, row, and new-trip form**

Replace `Planova/Features/TripList/TripListView.swift` with:

```swift
import SwiftUI
import PlanovaKit

struct TripListView: View {
    @Environment(AppModel.self) private var model
    @State private var showingNewTrip = false

    var body: some View {
        NavigationStack {
            List {
                ForEach(model.store.trips) { trip in
                    NavigationLink(value: trip.id) {
                        TripRowView(trip: trip)
                    }
                }
                .onDelete { offsets in
                    for index in offsets {
                        model.deleteTrip(id: model.store.trips[index].id)
                    }
                }
            }
            .navigationTitle("Trips")
            .navigationDestination(for: UUID.self) { id in
                // Replaced with TripDetailView in Task 10.
                Text(model.store.trip(id: id)?.name ?? "")
            }
            .toolbar {
                Button {
                    showingNewTrip = true
                } label: {
                    Image(systemName: "plus")
                }
            }
            .sheet(isPresented: $showingNewTrip) {
                NewTripView()
            }
        }
    }
}

struct TripRowView: View {
    let trip: Trip

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            Text(trip.name).font(.headline)
            HStack(spacing: 8) {
                Text(trip.startDate, format: .dateTime.year().month().day())
                Text("–")
                Text(trip.endDate, format: .dateTime.month().day())
                if !trip.items.isEmpty {
                    Spacer()
                    Text("\(trip.items.filter(\.isDone).count)/\(trip.items.count)")
                        .monospacedDigit()
                }
            }
            .font(.caption)
            .foregroundStyle(.secondary)
        }
        .padding(.vertical, 2)
    }
}

struct NewTripView: View {
    @Environment(AppModel.self) private var model
    @Environment(\.dismiss) private var dismiss
    @State private var name = ""
    @State private var start = Date()
    @State private var end = Date()

    var body: some View {
        NavigationStack {
            Form {
                TextField("Name", text: $name)
                DatePicker("Start", selection: $start, displayedComponents: .date)
                DatePicker("End", selection: $end, in: start..., displayedComponents: .date)
            }
            .navigationTitle("New Trip")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Save") { save() }
                        .disabled(name.trimmingCharacters(in: .whitespaces).isEmpty)
                }
            }
        }
    }

    private func save() {
        let calendar = Calendar.current
        var days: [TripDay] = []
        var date = calendar.startOfDay(for: start)
        let last = calendar.startOfDay(for: max(start, end))
        while date <= last {
            days.append(TripDay(date: date, city: "", title: ""))
            date = calendar.date(byAdding: .day, value: 1, to: date)!
        }
        model.addTrip(Trip(name: name, startDate: start, endDate: end, days: days))
        dismiss()
    }
}
```

- [ ] **Step 2: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Commit**

```bash
git add Planova
git commit -m "feat: add trip list and new-trip form"
```

---

### Task 9: ChecklistRow, EditorContext, ItemEditorView, ReminderScheduler

**Files:**
- Create: `Planova/Features/Checklist/ChecklistRow.swift`
- Create: `Planova/Features/ItemEditor/ItemEditorView.swift`
- Modify: `Planova/Reminders/ReminderScheduler.swift` (replace stub)

- [ ] **Step 1: Implement ChecklistRow and EditorContext**

`Planova/Features/Checklist/ChecklistRow.swift`:

```swift
import SwiftUI
import PlanovaKit

/// What the item editor sheet was opened for.
struct EditorContext: Identifiable {
    enum Mode {
        case add(ItemKind, dayID: UUID?)
        case edit(ChecklistItem)
    }
    let id = UUID()
    let mode: Mode
}

struct ChecklistRow: View {
    let item: ChecklistItem
    let onToggle: () -> Void
    let onEdit: () -> Void

    var body: some View {
        Button(action: onToggle) {
            HStack(alignment: .top, spacing: 12) {
                Image(systemName: item.isDone ? "checkmark.circle.fill" : "circle")
                    .font(.title3)
                    .foregroundStyle(item.isDone ? Color.green : Color.secondary)
                VStack(alignment: .leading, spacing: 3) {
                    HStack(spacing: 6) {
                        if let time = item.time, !time.isEmpty {
                            Text(time)
                                .font(.caption.weight(.semibold))
                                .padding(.horizontal, 6).padding(.vertical, 1)
                                .background(.teal.opacity(0.15), in: Capsule())
                                .foregroundStyle(.teal)
                        }
                        Text(item.label)
                            .strikethrough(item.isDone)
                            .foregroundStyle(item.isDone ? .secondary : .primary)
                        if let owner = item.owner, !owner.isEmpty {
                            Text(owner)
                                .font(.caption2)
                                .padding(.horizontal, 6).padding(.vertical, 1)
                                .background(.orange.opacity(0.15), in: Capsule())
                                .foregroundStyle(.orange)
                        }
                    }
                    if !item.notes.isEmpty {
                        Text(item.notes)
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                    if let reminder = item.reminderDate {
                        Label {
                            Text(reminder, format: .dateTime.month().day().hour().minute())
                        } icon: {
                            Image(systemName: "bell")
                        }
                        .font(.caption2)
                        .foregroundStyle(.teal)
                    }
                }
                Spacer(minLength: 0)
            }
            .contentShape(Rectangle())
        }
        .buttonStyle(.plain)
        .swipeActions(edge: .trailing, allowsFullSwipe: false) {
            Button("Edit", action: onEdit).tint(.blue)
        }
    }
}
```

- [ ] **Step 2: Implement ItemEditorView**

`Planova/Features/ItemEditor/ItemEditorView.swift`:

```swift
import SwiftUI
import PlanovaKit

struct ItemEditorView: View {
    @Environment(AppModel.self) private var model
    @Environment(\.dismiss) private var dismiss

    let context: EditorContext
    let trip: Trip

    @State private var label = ""
    @State private var notes = ""
    @State private var owner = ""
    @State private var dayID: UUID?
    @State private var hasTime = false
    @State private var timeValue = Date()
    @State private var hasReminder = false
    @State private var reminderDate = Date()
    @State private var loaded = false

    private var kind: ItemKind {
        switch context.mode {
        case .add(let kind, _): return kind
        case .edit(let item): return item.kind
        }
    }

    private var editedItem: ChecklistItem? {
        if case .edit(let item) = context.mode { return item }
        return nil
    }

    private static let timeFormatter: DateFormatter = {
        let f = DateFormatter()
        f.dateFormat = "HH:mm"
        return f
    }()

    var body: some View {
        NavigationStack {
            Form {
                Section {
                    TextField("Name", text: $label, axis: .vertical)
                    TextField("Notes", text: $notes, axis: .vertical)
                    if kind != .doc && kind != .hotel {
                        TextField("Owner", text: $owner)
                    }
                }
                if kind == .itinerary {
                    Section {
                        Picker("Day", selection: $dayID) {
                            ForEach(trip.days) { day in
                                Text("\(day.date, format: .dateTime.month().day()) \(day.title)")
                                    .tag(Optional(day.id))
                            }
                        }
                        Toggle("Time", isOn: $hasTime.animation())
                        if hasTime {
                            DatePicker("", selection: $timeValue, displayedComponents: .hourAndMinute)
                                .labelsHidden()
                        }
                    }
                }
                Section {
                    Toggle("Reminder", isOn: $hasReminder.animation())
                    if hasReminder {
                        DatePicker("", selection: $reminderDate, displayedComponents: [.date, .hourAndMinute])
                            .labelsHidden()
                    }
                }
                if editedItem != nil {
                    Section {
                        Button("Delete", role: .destructive) {
                            if let item = editedItem {
                                model.deleteItem(id: item.id, in: trip.id)
                            }
                            dismiss()
                        }
                    }
                }
            }
            .navigationTitle(editedItem == nil ? "New Item" : "Edit Item")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Save") { save() }
                        .disabled(label.trimmingCharacters(in: .whitespaces).isEmpty)
                }
            }
            .onAppear(perform: populate)
        }
    }

    private func populate() {
        guard !loaded else { return }
        loaded = true
        switch context.mode {
        case .add(_, let day):
            dayID = day ?? trip.days.first?.id
        case .edit(let item):
            label = item.label
            notes = item.notes
            owner = item.owner ?? ""
            dayID = item.dayID
            if let t = item.time, let parsed = Self.timeFormatter.date(from: t) {
                hasTime = true
                timeValue = parsed
            }
            if let r = item.reminderDate {
                hasReminder = true
                reminderDate = r
            }
        }
    }

    private func save() {
        let trimmedOwner = owner.trimmingCharacters(in: .whitespaces)
        let time = (kind == .itinerary && hasTime) ? Self.timeFormatter.string(from: timeValue) : nil
        let reminder = hasReminder ? reminderDate : nil

        if var item = editedItem {
            item.label = label
            item.notes = notes
            item.owner = trimmedOwner.isEmpty ? nil : trimmedOwner
            item.dayID = (kind == .itinerary) ? dayID : nil
            item.time = time
            item.reminderDate = reminder
            model.updateItem(item, in: trip.id)
        } else {
            let item = ChecklistItem(kind: kind, label: label, notes: notes,
                                     dayID: (kind == .itinerary) ? dayID : nil,
                                     time: time,
                                     owner: trimmedOwner.isEmpty ? nil : trimmedOwner,
                                     reminderDate: reminder)
            model.addItem(item, to: trip.id)
        }
        dismiss()
    }
}
```

- [ ] **Step 3: Implement the real ReminderScheduler**

Replace `Planova/Reminders/ReminderScheduler.swift` with:

```swift
import Foundation
import UserNotifications
import PlanovaKit

/// Reconciles pending local notifications with the current set of
/// future, not-done reminder items across all trips.
enum ReminderScheduler {

    static func refresh(trips: [Trip]) async {
        let center = UNUserNotificationCenter.current()
        let desired = ReminderPlanner.desiredReminders(for: trips, now: Date())

        if !desired.isEmpty {
            _ = try? await center.requestAuthorization(options: [.alert, .sound, .badge])
        }

        let pendingIDs = Set(await center.pendingNotificationRequests().map(\.identifier))
        let plan = ReminderPlanner.diff(desired: desired, pendingIDs: pendingIDs)

        center.removePendingNotificationRequests(withIdentifiers: plan.cancel)
        for reminder in plan.schedule {
            let content = UNMutableNotificationContent()
            content.title = reminder.title
            content.body = reminder.body
            content.sound = .default
            let components = Calendar.current.dateComponents(
                [.year, .month, .day, .hour, .minute], from: reminder.date)
            let trigger = UNCalendarNotificationTrigger(dateMatching: components, repeats: false)
            try? await center.add(UNNotificationRequest(
                identifier: reminder.id, content: content, trigger: trigger))
        }
    }
}
```

- [ ] **Step 4: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Commit**

```bash
git add Planova
git commit -m "feat: add checklist row, item editor, and notification scheduling"
```

---

### Task 10: Trip detail + Prep tab

**Files:**
- Create: `Planova/Features/TripDetail/TripDetailView.swift`
- Create: `Planova/Features/Checklist/PrepView.swift`
- Modify: `Planova/Features/TripList/TripListView.swift` (navigationDestination)

- [ ] **Step 1: Implement TripDetailView**

`Planova/Features/TripDetail/TripDetailView.swift`:

```swift
import SwiftUI
import PlanovaKit

struct TripDetailView: View {
    @Environment(AppModel.self) private var model
    let tripID: UUID

    var body: some View {
        if let trip = model.store.trip(id: tripID) {
            TabView {
                PrepView(trip: trip)
                    .tabItem { Label("Prep", systemImage: "checklist") }
                ItineraryView(trip: trip)
                    .tabItem { Label("Itinerary", systemImage: "calendar") }
                PackingView(trip: trip)
                    .tabItem { Label("Packing", systemImage: "bag") }
            }
            .navigationTitle(trip.name)
            .navigationBarTitleDisplayMode(.inline)
        } else {
            ContentUnavailableView("Trip not found", systemImage: "questionmark.circle")
        }
    }
}
```

Note: `ItineraryView` and `PackingView` are added in Tasks 11–12. To keep this task compiling on its own, also create temporary placeholders that those tasks replace:

`Planova/Features/Itinerary/ItineraryView.swift`:

```swift
import SwiftUI
import PlanovaKit

struct ItineraryView: View {
    let trip: Trip
    var body: some View { Text("Itinerary — Task 11") }
}
```

`Planova/Features/Packing/PackingView.swift`:

```swift
import SwiftUI
import PlanovaKit

struct PackingView: View {
    let trip: Trip
    var body: some View { Text("Packing — Task 12") }
}
```

- [ ] **Step 2: Implement PrepView**

`Planova/Features/Checklist/PrepView.swift`:

```swift
import SwiftUI
import PlanovaKit

struct PrepView: View {
    @Environment(AppModel.self) private var model
    let trip: Trip
    @State private var editing: EditorContext?

    var body: some View {
        List {
            section(kind: .prep, title: "Bookings")
            section(kind: .hotel, title: "Hotels")
            section(kind: .doc, title: "Documents")
        }
        .sheet(item: $editing) { context in
            ItemEditorView(context: context, trip: trip)
        }
    }

    @ViewBuilder
    private func section(kind: ItemKind, title: LocalizedStringKey) -> some View {
        let items = trip.items
            .filter { $0.kind == kind }
            .sorted { $0.sortOrder < $1.sortOrder }
        Section {
            ForEach(items) { item in
                ChecklistRow(item: item,
                             onToggle: { model.toggle(itemID: item.id, in: trip.id) },
                             onEdit: { editing = EditorContext(mode: .edit(item)) })
            }
            Button {
                editing = EditorContext(mode: .add(kind, dayID: nil))
            } label: {
                Label("Add Item", systemImage: "plus")
            }
        } header: {
            HStack {
                Text(title)
                Spacer()
                Text("\(items.filter(\.isDone).count)/\(items.count)").monospacedDigit()
            }
        }
    }
}
```

- [ ] **Step 3: Wire the navigation destination**

In `Planova/Features/TripList/TripListView.swift`, replace:

```swift
            .navigationDestination(for: UUID.self) { id in
                // Replaced with TripDetailView in Task 10.
                Text(model.store.trip(id: id)?.name ?? "")
            }
```

with:

```swift
            .navigationDestination(for: UUID.self) { id in
                TripDetailView(tripID: id)
            }
```

- [ ] **Step 4: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Commit**

```bash
git add Planova
git commit -m "feat: add trip detail tabs and prep checklist"
```

---

### Task 11: Itinerary tab

**Files:**
- Modify: `Planova/Features/Itinerary/ItineraryView.swift` (replace placeholder)

- [ ] **Step 1: Implement the day accordion**

Replace `Planova/Features/Itinerary/ItineraryView.swift` with:

```swift
import SwiftUI
import PlanovaKit

struct ItineraryView: View {
    @Environment(AppModel.self) private var model
    let trip: Trip
    @State private var expanded: Set<UUID> = []
    @State private var didSetInitialExpansion = false
    @State private var editing: EditorContext?

    var body: some View {
        List {
            ForEach(trip.days) { day in
                daySection(day)
            }
        }
        .listStyle(.sidebar)
        .sheet(item: $editing) { context in
            ItemEditorView(context: context, trip: trip)
        }
        .onAppear(perform: setInitialExpansion)
    }

    @ViewBuilder
    private func daySection(_ day: TripDay) -> some View {
        let items = ItinerarySort.daySorted(
            trip.items
                .filter { $0.dayID == day.id }
                .sorted { $0.sortOrder < $1.sortOrder }
        )
        Section(isExpanded: expansionBinding(for: day.id)) {
            ForEach(items) { item in
                ChecklistRow(item: item,
                             onToggle: { model.toggle(itemID: item.id, in: trip.id) },
                             onEdit: { editing = EditorContext(mode: .edit(item)) })
            }
            Button {
                editing = EditorContext(mode: .add(.itinerary, dayID: day.id))
            } label: {
                Label("Add Item", systemImage: "plus")
            }
        } header: {
            DayHeader(day: day,
                      done: items.filter(\.isDone).count,
                      total: items.count,
                      isToday: Calendar.current.isDateInToday(day.date))
        }
    }

    private func expansionBinding(for id: UUID) -> Binding<Bool> {
        Binding(
            get: { expanded.contains(id) },
            set: { isOpen in
                if isOpen { expanded.insert(id) } else { expanded.remove(id) }
            }
        )
    }

    private func setInitialExpansion() {
        guard !didSetInitialExpansion else { return }
        didSetInitialExpansion = true
        if let today = trip.days.first(where: { Calendar.current.isDateInToday($0.date) }) {
            expanded = [today.id]
        } else if let first = trip.days.first {
            expanded = [first.id]
        }
    }
}

struct DayHeader: View {
    let day: TripDay
    let done: Int
    let total: Int
    let isToday: Bool

    var body: some View {
        HStack(spacing: 10) {
            Text(day.date, format: .dateTime.month(.abbreviated).day())
                .font(.subheadline.weight(.bold))
            VStack(alignment: .leading, spacing: 1) {
                if !day.title.isEmpty {
                    Text(day.title).font(.subheadline)
                }
                if !day.city.isEmpty {
                    Text(day.city).font(.caption2).foregroundStyle(.secondary)
                }
            }
            if isToday {
                Text("Today")
                    .font(.caption2.weight(.bold))
                    .padding(.horizontal, 6).padding(.vertical, 1)
                    .background(.teal, in: Capsule())
                    .foregroundStyle(.white)
            }
            Spacer()
            Text("\(done)/\(total)")
                .font(.caption)
                .monospacedDigit()
                .foregroundStyle(done == total && total > 0 ? .green : .secondary)
        }
        .textCase(nil)
    }
}
```

- [ ] **Step 2: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Commit**

```bash
git add Planova
git commit -m "feat: add itinerary day accordion with today highlight"
```

---

### Task 12: Packing tab

**Files:**
- Modify: `Planova/Features/Packing/PackingView.swift` (replace placeholder)

- [ ] **Step 1: Implement the packing checklist**

Replace `Planova/Features/Packing/PackingView.swift` with:

```swift
import SwiftUI
import PlanovaKit

struct PackingView: View {
    @Environment(AppModel.self) private var model
    let trip: Trip
    @State private var editing: EditorContext?

    private var items: [ChecklistItem] {
        trip.items
            .filter { $0.kind == .packing }
            .sorted { $0.sortOrder < $1.sortOrder }
    }

    var body: some View {
        List {
            Section {
                ForEach(items) { item in
                    ChecklistRow(item: item,
                                 onToggle: { model.toggle(itemID: item.id, in: trip.id) },
                                 onEdit: { editing = EditorContext(mode: .edit(item)) })
                }
                Button {
                    editing = EditorContext(mode: .add(.packing, dayID: nil))
                } label: {
                    Label("Add Item", systemImage: "plus")
                }
            } header: {
                HStack {
                    Text("Packing")
                    Spacer()
                    Text("\(items.filter(\.isDone).count)/\(items.count)").monospacedDigit()
                }
            }
            Section {
                Button {
                    model.resetPacking(in: trip.id)
                } label: {
                    Label("Reset packing list", systemImage: "arrow.counterclockwise")
                }
            }
        }
        .sheet(item: $editing) { context in
            ItemEditorView(context: context, trip: trip)
        }
    }
}
```

- [ ] **Step 2: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Commit**

```bash
git add Planova
git commit -m "feat: add packing checklist with daily reset"
```

---

### Task 13: Localization (zh-Hans + English)

**Files:**
- Create: `Planova/Resources/Localizable.xcstrings`

- [ ] **Step 1: Create the string catalog**

SwiftUI `Text("...")`, `Label("...")`, `TextField("...")`, navigation titles, and `Button("...")` literals are localization keys automatically. Create `Planova/Resources/Localizable.xcstrings` with translations for every user-facing key used in Tasks 8–12:

```json
{
  "sourceLanguage" : "en",
  "version" : "1.0",
  "strings" : {
    "Trips" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "行程" } } } },
    "New Trip" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "新建行程" } } } },
    "Name" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "名称" } } } },
    "Start" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "开始日期" } } } },
    "End" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "结束日期" } } } },
    "Save" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "保存" } } } },
    "Cancel" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "取消" } } } },
    "Delete" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "删除" } } } },
    "Edit" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "编辑" } } } },
    "Prep" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "行前准备" } } } },
    "Itinerary" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "每日行程" } } } },
    "Packing" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "出门清单" } } } },
    "Bookings" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "关键预订" } } } },
    "Hotels" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "住宿" } } } },
    "Documents" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "证件 · 出行准备" } } } },
    "Add Item" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "添加项目" } } } },
    "New Item" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "新增项目" } } } },
    "Edit Item" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "编辑项目" } } } },
    "Notes" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "备注" } } } },
    "Owner" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "负责人" } } } },
    "Day" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "日期" } } } },
    "Time" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "时间" } } } },
    "Reminder" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "提醒" } } } },
    "Reset packing list" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "重置每日清单" } } } },
    "Today" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "今日" } } } },
    "Trip not found" : { "localizations" : { "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "未找到行程" } } } }
  }
}
```

- [ ] **Step 2: Declare supported localizations in project.yml**

In `project.yml`, add to the `Planova` target's `settings.base`:

```yaml
        INFOPLIST_KEY_CFBundleLocalizations: "en zh-Hans"
```

(Keep all existing settings; this is one added line.)

- [ ] **Step 3: Build**

Run:
```bash
xcodegen generate && xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 4: Commit**

```bash
git add project.yml Planova/Resources
git commit -m "feat: add zh-Hans localization via string catalog"
```

---

### Task 14: Final verification and wrap-up

**Files:** none created — verification only.

- [ ] **Step 1: Run the full PlanovaKit test suite**

Run: `cd PlanovaKit && swift test`
Expected: PASS — 17 tests, 0 failures.

- [ ] **Step 2: Clean build of the app**

Run:
```bash
xcodegen generate
xcodebuild -project Planova.xcodeproj -scheme Planova -destination 'generic/platform=iOS Simulator' clean build
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Manual smoke test in the simulator**

Boot a simulator and install:

```bash
xcrun simctl list devices available | grep -m1 iPhone
# use the device name found above, e.g.:
xcodebuild -project Planova.xcodeproj -scheme Planova \
  -destination 'platform=iOS Simulator,name=iPhone 16' build
xcrun simctl boot "iPhone 16" || true
open -a Simulator
APP=$(find ~/Library/Developer/Xcode/DerivedData -path '*Build/Products/Debug-iphonesimulator/Planova.app' | head -1)
xcrun simctl install booted "$APP"
xcrun simctl launch booted com.planova.Planova
```

Verify by hand:
1. Trip list shows "2026 暑假中国行" with progress `1/188` (the booked hotel).
2. Prep tab: three sections with correct counts (Bookings 0/5, Hotels 1/6, Documents 0/5); tapping a row toggles it; swipe → Edit opens the editor.
3. Itinerary tab: 21 collapsible days; adding an item with time `09:00` to a day sorts it to the top of that day.
4. Packing tab: check two items, tap Reset packing list, both clear.
5. Editor: set a Reminder ~2 minutes in the future on any item, background the app → notification fires (first time, iOS asks permission).
6. Kill and relaunch the app → all changes persisted.
7. Device language Chinese (Settings → General → Language) → UI chrome appears in Chinese.

- [ ] **Step 4: Commit any fixes, then tag**

```bash
git status   # commit fixes if any step above required changes
git tag v0.1.0-core
```

---

## Follow-up plans (not in this document)

1. **Plan 2 — CloudKit sync + family sharing:** `CKSyncEngine` private/shared databases, one custom zone per trip, per-item records, zone-wide `CKShare` + `UICloudSharingController`. Requires the Apple Developer account (CloudKit entitlement + container).
2. **Plan 3 — Maps + TestFlight:** `MKLocalSearch` place attachment in the item editor, per-item open-in-Maps, per-day pin map, app icon, signing, TestFlight upload.
