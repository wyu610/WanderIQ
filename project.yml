name: WanderIQ
options:
  # App Store Connect app 6779690103 is registered as com.WanderIQ (prefix +
  # target name); the iCloud container keeps iCloud.com.wanderiq.WanderIQ.
  bundleIdPrefix: com
  deploymentTarget:
    iOS: "17.0"
configFiles:
  Debug: Signing.xcconfig
  Release: Signing.xcconfig
packages:
  WanderIQKit:
    path: WanderIQKit
targets:
  WanderIQ:
    type: application
    platform: iOS
    sources: [WanderIQ]
    dependencies:
      - package: WanderIQKit
    scheme:
      testTargets: [WanderIQUITests]
    entitlements:
      path: WanderIQ.entitlements
      properties:
        com.apple.developer.icloud-container-identifiers: [iCloud.com.wanderiq.WanderIQ]
        com.apple.developer.icloud-services: [CloudKit]
        aps-environment: development
    info:
      path: WanderIQ/Info.plist
      properties:
        UILaunchScreen: {}
        CFBundleDisplayName: WanderIQ
        CFBundleLocalizations: [en, zh-Hans]
        UIBackgroundModes: [remote-notification]
        # Wire versions to build settings; XcodeGen otherwise hardcodes 1.0/1
        # and MARKETING_VERSION never reaches the binary.
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        # Only HTTPS/iCloud (exempt) encryption; skips the ASC export-compliance
        # prompt on every upload.
        ITSAppUsesNonExemptEncryption: false
        # ASC upload validation (error 90474) requires these; iPad must list
        # all four orientations because the app doesn't require full screen.
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait, UIInterfaceOrientationLandscapeLeft, UIInterfaceOrientationLandscapeRight]
        UISupportedInterfaceOrientations~ipad: [UIInterfaceOrientationPortrait, UIInterfaceOrientationPortraitUpsideDown, UIInterfaceOrientationLandscapeLeft, UIInterfaceOrientationLandscapeRight]
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1,2"
        SWIFT_VERSION: "5.10"
        CURRENT_PROJECT_VERSION: 1
        # 1.0 (1) is what actually shipped to TestFlight on 2026-06-12 (the
        # old 0.3.0 here never reached binaries). Bump CURRENT_PROJECT_VERSION
        # for each new upload of the same marketing version.
        MARKETING_VERSION: "1.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
  WanderIQUITests:
    type: bundle.ui-testing
    platform: iOS
    sources: [WanderIQUITests]
    dependencies:
      - target: WanderIQ
    settings:
      base:
        GENERATE_INFOPLIST_FILE: true
